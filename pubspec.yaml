name: flutter_testing_example
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  golden_toolkit: ^0.15.0
  mockito: ^5.4.4

dev_dependencies:
  flutter_lints: ^3.0.0
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
